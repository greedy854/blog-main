{
  "name": "react-blog",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^9.1.0",
    "cors": "^2.8.5",
    "cra-template": "1.2.0",
    "json-server": "^1.0.0-beta.3",
    "path-browserify": "^1.0.1",
    "react": "^18.0.0",
    "react-app-rewired": "^2.2.1",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.1",
    "serve": "^14.2.4"
  },
  "scripts": {
    "start": "concurrently \"react-app-rewired start\" \"json-server --watch db.json --port 5000\"",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-app-rewired eject",
    "server": "json-server --watch db.json --port 5000",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "homepage": ".",
  "engines": {
    "node": "16.x"
  },
  "devDependencies": {
    "net-browserify": "^0.2.4",
    "stream-http": "^3.2.0"
  }
}
